{
  "name": "portfolio-site",
  "version": "1.0.0",
  "description": "Pavlo Pukha 3D Artist Portfolio",
  "type": "module",
  "scripts": {
    "build": "esbuild analytics.js --bundle --outfile=analytics.bundle.js --format=esm --minify",
    "dev": "npm run build && node static-server.mjs",
    "preview": "node static-server.mjs"
  },
  "keywords": [
    "portfolio",
    "3d-artist"
  ],
  "author": "Pavlo Pukha",
  "license": "MIT",
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
